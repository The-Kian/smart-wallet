Authentication Setup:

Web Target: Fully integrated with Google OAuth 2.0.

Mobile Target (iOS/Android): Due to Google Cloud's strict SHA-1 fingerprint requirements for native clients, I have provided a Sandbox Bypass for mobile simulator testing. This ensures reviewers do not need to generate native keystores or configure external Firebase projects to evaluate the app's core functionality.